# Telegram Бот для Управления Задачами (без AI)

## Описание

Простой Telegram бот для управления задачами с системой ролей. Бот работает по командам (без использования AI) - полностью бесплатно!

## Возможности

### Для Администраторов
- ✅ Создание задач с описанием, приоритетом и сроками
- 📋 Просмотр всех задач
- 👥 Назначение задач сотрудникам

### Для Сотрудников
- 📬 Просмотр назначенных задач
- 🔄 Изменение статуса задач (в работе, завершена, отклонена)
- 👀 Просмотр деталей задачи

## Настройка

### 1. Публикация приложения

Чтобы Telegram webhook заработал, необходимо опубликовать приложение:

1. Нажмите кнопку **"Publish"** в Replit
2. Дождитесь завершения публикации
3. Скопируйте публичный URL (будет в формате `https://ваш-проект.replit.app`)

### 2. Регистрация Telegram Webhook

После публикации выполните следующую команду в Shell (замените `YOUR_PUBLIC_URL`):

```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"YOUR_PUBLIC_URL/webhooks/telegram/action\"}"
```

Пример:
```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"https://telegram-task-bot.replit.app/webhooks/telegram/action\"}"
```

### 3. Проверка webhook

Проверьте, что webhook установлен правильно:

```bash
curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
```

## Список Команд

### Общие команды

#### `/start`
Регистрация в системе или приветствие для существующих пользователей.

**Пример:**
```
/start
```

#### `/help` или `/помощь`
Показывает список доступных команд в зависимости от вашей роли.

**Пример:**
```
/help
```

### Команды для работы с задачами

#### `/my_tasks` или `/мои_задачи`
Показывает список ваших назначенных задач.

**Пример:**
```
/my_tasks
```

**Ответ:**
```
📋 Ваши задачи (2):

📌 ID 1: Подготовить отчет
   Статус: in_progress
   Приоритет: high
   Срок: 2025-12-25

📌 ID 2: Проверить данные
   Статус: pending
   Приоритет: medium
   Срок: 2025-12-20
```

#### `/task_details <ID>` или `/детали_задачи <ID>`
Показывает подробную информацию о задаче.

**Пример:**
```
/task_details 5
```

**Ответ:**
```
📋 Детали задачи #5

Название: Подготовить отчет
Описание: Квартальный отчет по продажам
Статус: in_progress
Приоритет: high
Срок: 2025-12-25
Назначена: User #123
Создана: 2025-11-15
```

#### `/update_status <ID> <статус>` или `/обновить_статус <ID> <статус>`
Обновляет статус задачи.

**Доступные статусы:**
- `pending` - ожидает
- `in_progress` - в работе
- `completed` - завершена
- `rejected` - отклонена

**Пример:**
```
/update_status 5 in_progress
```

**Ответ:**
```
✅ Статус задачи #5 обновлён на: in_progress
```

### Команды администратора

#### `/create_task` или `/создать_задачу`
Создает новую задачу. Доступно только администраторам.

**Формат:**
```
/create_task title:"название" description:"описание" priority:приоритет due_date:YYYY-MM-DD assigned_to:telegram_id
```

**Параметры:**
- `title:"..."` - название задачи (обязательно)
- `description:"..."` - описание (опционально)
- `priority:` - приоритет: low/medium/high/urgent (по умолчанию: medium)
- `due_date:` - срок в формате YYYY-MM-DD (опционально)
- `assigned_to:` - telegram_id сотрудника (опционально)

**Примеры:**

Простая задача:
```
/create_task title:"Подготовить отчет"
```

Полная задача:
```
/create_task title:"Подготовить отчет" description:"Квартальный отчет по продажам" priority:high due_date:2025-12-25
```

Задача с назначением:
```
/create_task title:"Проверить данные" assigned_to:123456789 priority:urgent
```

**Ответ:**
```
✅ Задача создана успешно!

ID: 6
Название: Подготовить отчет
Приоритет: high
Статус: pending
```

#### `/all_tasks` или `/все_задачи`
Показывает все задачи в системе. Доступно только администраторам.

**Пример:**
```
/all_tasks
```

**Ответ:**
```
📋 Все задачи (5):

📌 ID 1: Подготовить отчет
   Статус: completed
   Приоритет: high
   Назначена: User #123

📌 ID 2: Проверить данные
   Статус: in_progress
   Приоритет: medium
   Назначена: User #456
```

## Получение роли администратора

По умолчанию все новые пользователи регистрируются как **сотрудники**.

Чтобы получить роль администратора, выполните SQL запрос в базе данных:

```sql
UPDATE users SET role = 'admin' WHERE telegram_id = 'ваш_telegram_id';
```

Ваш Telegram ID можно узнать:
1. Отправьте `/start` боту
2. Посмотрите в базе данных таблицу `users`
3. Найдите свою запись по username

## Архитектура

### Компоненты

1. **База данных PostgreSQL**
   - Таблица `users`: хранение пользователей с ролями
   - Таблица `tasks`: хранение задач с приоритетами и статусами

2. **Command Handler**
   - Парсинг команд Telegram
   - Валидация прав доступа
   - Выполнение операций с базой данных

3. **Workflow (Mastra + Inngest)**
   - Прием сообщений от Telegram webhook
   - Обработка команд
   - Отправка ответа пользователю

### Схема данных

**Users:**
- `id`: уникальный идентификатор
- `telegram_id`: ID пользователя в Telegram (уникальный)
- `username`: имя пользователя
- `role`: admin | employee
- `created_at`: дата регистрации

**Tasks:**
- `id`: уникальный идентификатор
- `title`: название задачи
- `description`: описание
- `priority`: low | medium | high | urgent
- `status`: pending | in_progress | completed | rejected
- `due_date`: срок выполнения
- `assigned_to_id`: назначена сотруднику (foreign key)
- `created_by_id`: создана администратором (foreign key)

## Тестирование

### Тестирование через CURL

```bash
curl -X POST http://localhost:5000/webhooks/telegram/action \
  -H "Content-Type: application/json" \
  -d '{
    "message": {
      "text": "/start",
      "from": {
        "id": 123456789,
        "username": "test_user",
        "first_name": "Тест"
      },
      "chat": {
        "id": 123456789
      }
    }
  }'
```

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что webhook установлен:
   ```bash
   curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
   ```

2. Проверьте логи в Replit Console

3. Убедитесь, что приложение опубликовано и доступно

### Изменения не отражаются

После любых изменений в коде бота необходимо:

1. Дождаться hot-reload в консоли
2. Или перезапустить workflow вручную
3. **Переопубликовать приложение** через кнопку Publish

### База данных

Если нужно сбросить схему базы данных:

```bash
npm run db:push --force
```

## Преимущества (без AI)

✅ **Полностью бесплатно** - не требует оплаты API OpenAI
✅ **Быстрая работа** - мгновенная обработка команд
✅ **Предсказуемость** - чёткие команды, нет двусмысленности
✅ **Простота** - легко понять и использовать

## Следующие шаги

- 📊 Добавьте статистику по задачам
- 📅 Реализуйте напоминания о сроках
- 🔍 Добавьте фильтрацию задач по статусу
- 📈 Добавьте отчёты для администраторов
- 🌍 Добавьте поддержку других языков

## Технические детали

- **Framework**: Mastra v0.20.0 (без AI агентов)
- **Database**: PostgreSQL с Drizzle ORM
- **Workflow Engine**: Inngest
- **Platform**: Replit
- **Cost**: Полностью бесплатно (без затрат на AI)

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все environment variables установлены
3. Проверьте статус webhook
4. Обратитесь к документации Mastra: https://mastra.ai

# Telegram Бот для Управления Задачами

## Описание

Telegram бот с AI-агентом для управления задачами с системой ролей. Бот позволяет администраторам создавать задачи, а сотрудникам - получать и выполнять их.

## Возможности

### Для Администраторов
- ✅ Создание задач с описанием, приоритетом (низкий/средний/высокий/срочный) и сроками
- 📋 Просмотр всех задач за определенный период
- 🔔 Отправка повторных уведомлений сотрудникам
- 👥 Назначение задач конкретным сотрудникам

### Для Сотрудников
- 📬 Получение назначенных задач
- 👀 Просмотр своих задач
- 🔄 Изменение статуса задач:
  - В работе
  - Завершена
  - Не принята (отклонена)

## Настройка

### 1. Публикация приложения

Чтобы Telegram webhook заработал, необходимо опубликовать приложение:

1. Нажмите кнопку **"Publish"** в Replit
2. Дождитесь завершения публикации
3. Скопируйте публичный URL (будет в формате `https://ваш-проект.replit.app`)

### 2. Регистрация Telegram Webhook

После публикации выполните следующую команду в Shell (замените `YOUR_PUBLIC_URL`):

```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"YOUR_PUBLIC_URL/webhooks/telegram/action\"}"
```

Пример:
```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"https://telegram-task-bot.replit.app/webhooks/telegram/action\"}"
```

### 3. Проверка webhook

Проверьте, что webhook установлен правильно:

```bash
curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
```

## Использование

### Начало работы

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start` или любое сообщение
3. Бот автоматически создаст профиль пользователя с ролью **employee** (сотрудник)

### Получение роли администратора

По умолчанию все новые пользователи получают роль сотрудника. Чтобы получить роль администратора, выполните SQL запрос в базе данных:

```sql
UPDATE users SET role = 'admin' WHERE telegram_id = 'ваш_telegram_id';
```

Или используйте Playground для выполнения операций от имени администратора.

### Примеры команд

#### Создание задачи (администратор)
```
Создай задачу "Подготовить отчет по продажам за квартал" с высоким приоритетом, срок 25 декабря
```

#### Просмотр задач
```
Покажи мои задачи
```

#### Обновление статуса задачи
```
Отметь задачу #5 как завершенную
```

#### Просмотр всех задач (администратор)
```
Покажи все задачи за последнюю неделю
```

## Архитектура

### Компоненты

1. **База данных PostgreSQL**
   - Таблица `users`: хранение пользователей с ролями
   - Таблица `tasks`: хранение задач с приоритетами и статусами

2. **AI Агент (GPT-5)**
   - Обработка естественного языка
   - Понимание команд на русском языке
   - Память диалога для каждого пользователя

3. **Mastra Tools**
   - `userManagementTool`: управление пользователями
   - `taskManagementTool`: CRUD операции с задачами
   - `telegramNotificationTool`: отправка уведомлений

4. **Workflow (Inngest)**
   - Прием сообщений от Telegram
   - Обработка AI агентом
   - Отправка ответа пользователю

### Схема данных

**Users:**
- `id`: уникальный идентификатор
- `telegram_id`: ID пользователя в Telegram
- `username`: имя пользователя
- `role`: admin | employee
- `created_at`: дата регистрации

**Tasks:**
- `id`: уникальный идентификатор
- `title`: название задачи
- `description`: описание
- `priority`: low | medium | high | urgent
- `status`: pending | in_progress | completed | rejected
- `due_date`: срок выполнения
- `assigned_to`: назначена сотруднику
- `created_by`: создана администратором

## Тестирование

### Тестирование в Playground

1. Откройте вкладку **Playground** в Replit
2. Выберите workflow `telegram-task-workflow`
3. Выполните тестовый запрос с данными:

```json
{
  "threadId": "test-thread-1",
  "messageText": "Привет, покажи мои задачи",
  "telegramId": "123456789",
  "firstName": "Иван",
  "username": "ivan_test",
  "chatId": "123456789",
  "botToken": "your-bot-token-here"
}
```

### Тестирование через CURL

```bash
curl -X POST http://localhost:5000/api/workflows/telegram-task-workflow/run \
  -H "Content-Type: application/json" \
  -d '{
    "threadId": "test-thread-1",
    "messageText": "Создай задачу с высоким приоритетом",
    "telegramId": "123456789",
    "firstName": "Иван",
    "username": "ivan_test",
    "chatId": "123456789",
    "botToken": "'"${TELEGRAM_BOT_TOKEN}"'"
  }'
```

## Логирование

Бот использует структурированное логирование (Pino) для отладки:

- 🚀 Начало обработки сообщения
- 🔧 Выполнение tools
- ✅ Успешное завершение
- ❌ Ошибки и исключения

Просматривайте логи в консоли Replit для диагностики проблем.

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что webhook установлен:
   ```bash
   curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
   ```

2. Проверьте логи в Replit Console

3. Убедитесь, что приложение опубликовано и доступно

### База данных

Если нужно сбросить схему базы данных:

```bash
npm run db:push --force
```

### Изменения не отражаются

После любых изменений в коде бота необходимо:

1. Дождаться hot-reload в консоли
2. Или перезапустить workflow вручную
3. **Переопубликовать приложение** через кнопку Publish

## Следующие шаги

- 📊 Добавьте панель статистики для администраторов
- 📅 Реализуйте автоматические напоминания о сроках
- 🔍 Добавьте поиск задач по ключевым словам
- 📈 Интегрируйте аналитику выполнения задач
- 🌍 Добавьте поддержку других языков

## Технические детали

- **Framework**: Mastra v0.20.0
- **LLM**: OpenAI GPT-5 (через Replit AI Integrations)
- **Database**: PostgreSQL с Drizzle ORM
- **Workflow Engine**: Inngest
- **Platform**: Replit

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все environment variables установлены
3. Проверьте статус webhook
4. Обратитесь к документации Mastra: https://mastra.ai

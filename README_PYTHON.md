# Python Telegram Бот для Управления Задачами

## Описание

Простой Telegram бот на Python Flask для управления задачами с системой ролей. Бот работает по командам (без использования AI) - **полностью бесплатно**!

## Возможности

### Для Администраторов
- ✅ Создание задач с описанием, приоритетом и сроками
- 📋 Просмотр всех задач
- 👥 Назначение задач сотрудникам

### Для Сотрудников
- 📬 Просмотр назначенных задач
- 🔄 Изменение статуса задач (в работе, завершена, отклонена)
- 👀 Просмотр деталей задачи

## Установка и Запуск

### 1. Установка зависимостей

Зависимости уже установлены через `uv`:
- Flask
- psycopg2-binary
- requests
- python-dotenv

### 2. Environment Variables

Необходимые переменные окружения:
```bash
DATABASE_URL=<PostgreSQL connection string>
TELEGRAM_BOT_TOKEN=<ваш Telegram bot token>
```

### 3. Запуск бота локально

```bash
python app.py
```

Бот запустится на `http://0.0.0.0:5000`

### 4. Публикация (для production)

1. Нажмите кнопку **"Publish"** в Replit
2. Дождитесь завершения публикации
3. Скопируйте публичный URL

### 5. Регистрация Telegram Webhook

После публикации выполните:

```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{\"url\": \"https://ваш-проект.replit.app/webhooks/telegram/action\"}"
```

Проверка webhook:
```bash
curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
```

## Список Команд

### Общие команды

#### `/start`
Регистрация в системе или приветствие для существующих пользователей.

#### `/help` или `/помощь`
Показывает список доступных команд в зависимости от вашей роли.

### Команды для работы с задачами

#### `/my_tasks` или `/мои_задачи`
Показывает список ваших назначенных задач.

**Пример:**
```
/my_tasks
```

#### `/task_details <ID>` или `/детали_задачи <ID>`
Показывает подробную информацию о задаче.

**Пример:**
```
/task_details 5
```

#### `/update_status <ID> <статус>` или `/обновить_статус <ID> <статус>`
Обновляет статус задачи.

**Доступные статусы:**
- `pending` - ожидает
- `in_progress` - в работе
- `completed` - завершена
- `rejected` - отклонена

**Пример:**
```
/update_status 5 in_progress
```

### Команды администратора

#### `/create_task` или `/создать_задачу`
Создает новую задачу. Доступно только администраторам.

**Формат:**
```
/create_task title:"название" description:"описание" priority:приоритет due_date:YYYY-MM-DD assigned_to:telegram_id
```

**Параметры:**
- `title:"..."` - название задачи (обязательно)
- `description:"..."` - описание (опционально)
- `priority:` - приоритет: low/medium/high/urgent (по умолчанию: medium)
- `due_date:` - срок в формате YYYY-MM-DD (опционально)
- `assigned_to:` - telegram_id сотрудника (опционально)

**Примеры:**

Простая задача:
```
/create_task title:"Подготовить отчет"
```

Полная задача:
```
/create_task title:"Подготовить отчет" description:"Квартальный отчет" priority:high due_date:2025-12-25
```

Задача с назначением:
```
/create_task title:"Проверить данные" assigned_to:123456789 priority:urgent
```

#### `/all_tasks` или `/все_задачи`
Показывает все задачи в системе. Доступно только администраторам.

## Получение роли администратора

По умолчанию все новые пользователи регистрируются как **сотрудники**.

Чтобы получить роль администратора, выполните SQL запрос:

```sql
UPDATE users SET role = 'admin' WHERE telegram_id = 'ваш_telegram_id';
```

Ваш Telegram ID можно узнать:
1. Отправьте `/start` боту
2. Посмотрите в базе данных таблицу `users`
3. Найдите свою запись по username

## Архитектура

### Компоненты

1. **Flask Application** (`app.py`)
   - Webhook endpoint для Telegram
   - Обработка команд
   - Отправка ответов через Telegram Bot API

2. **Database (PostgreSQL)**
   - Таблица `users`: хранение пользователей с ролями
   - Таблица `tasks`: хранение задач с приоритетами и статусами

3. **Command Handler**
   - Парсинг команд Telegram
   - Валидация прав доступа
   - Выполнение операций с базой данных

### Схема данных

**Users:**
- `id`: уникальный идентификатор
- `telegram_id`: ID пользователя в Telegram (уникальный)
- `username`: имя пользователя
- `role`: admin | employee
- `created_at`: дата регистрации

**Tasks:**
- `id`: уникальный идентификатор
- `title`: название задачи
- `description`: описание
- `priority`: low | medium | high | urgent
- `status`: pending | in_progress | completed | rejected
- `due_date`: срок выполнения
- `assigned_to_id`: назначена сотруднику (foreign key)
- `created_by_id`: создана администратором (foreign key)

## Локальное тестирование

Тестирование через CURL (без публикации):

```bash
curl -X POST http://localhost:5000/webhooks/telegram/action \
  -H "Content-Type: application/json" \
  -d '{
    "message": {
      "text": "/start",
      "from": {
        "id": 123456789,
        "username": "test_user",
        "first_name": "Тест"
      },
      "chat": {
        "id": 123456789
      }
    }
  }'
```

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что webhook установлен:
   ```bash
   curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
   ```

2. Проверьте логи:
   ```bash
   tail -f /tmp/bot.log
   ```

3. Убедитесь, что приложение опубликовано и доступно

### Изменения не отражаются

После любых изменений в коде:
1. Перезапустите приложение
2. **Переопубликуйте** через кнопку Publish

### База данных

Если нужно сбросить схему базы данных:

```bash
npm run db:push --force
```

## Преимущества

✅ **Полностью бесплатно** - не требует оплаты AI API  
✅ **Простота** - только Python + Flask, без сложных фреймворков  
✅ **Быстрая работа** - мгновенная обработка команд  
✅ **Предсказуемость** - чёткие команды, нет двусмысленности  
✅ **Легко поддерживать** - один файл `app.py` с понятной логикой  

## Технические детали

- **Language**: Python 3.11
- **Framework**: Flask 3.1.2
- **Database**: PostgreSQL с psycopg2
- **Platform**: Replit
- **Cost**: Полностью бесплатно (без затрат на AI)

## Файловая структура

```
.
├── app.py                  # Основное приложение Flask
├── shared/
│   ├── schema.ts          # Схема БД (Drizzle)
│   └── db.ts              # Подключение к БД
├── README_PYTHON.md        # Эта документация
└── requirements.txt        # (генерируется uv)
```

## Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f /tmp/bot.log`
2. Убедитесь, что все environment variables установлены
3. Проверьте статус webhook
4. Убедитесь, что Flask запущен на порту 5000

# Telegram Бот для Управления Задачами (Python + Aiogram)

## Описание

Полноценный асинхронный Telegram бот на Python с использованием библиотеки **aiogram**. Бот работает по командам (без AI) и использует **polling** вместо webhook - **полностью бесплатно**!

## Возможности

### Для Администраторов
- ✅ Создание задач с описанием, приоритетом и сроками
- 📋 Просмотр всех задач в системе
- 👥 Назначение задач сотрудникам по Telegram ID
- 🔄 Обновление статусов любых задач

### Для Сотрудников
- 📬 Просмотр своих назначенных задач
- 🔄 Изменение статуса своих задач
- 👀 Просмотр деталей задач

## Установка и Запуск

### 1. Зависимости

Установлены через `uv`:
- **aiogram** 3.22.0 - асинхронная библиотека для Telegram Bot API
- **psycopg2-binary** - PostgreSQL драйвер
- **asyncpg** - асинхронный PostgreSQL драйвер
- **python-dotenv** - загрузка environment variables

### 2. Environment Variables

Необходимые переменные окружения:
```bash
DATABASE_URL=<PostgreSQL connection string>
TELEGRAM_BOT_TOKEN=<ваш Telegram bot token>
```

### 3. Запуск бота

```bash
python bot.py
```

Бот будет работать в режиме **polling** (постоянно опрашивает Telegram API) - не нужно настраивать webhook!

### 4. Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен
5. Добавьте токен в Secrets как `TELEGRAM_BOT_TOKEN`

## Список Команд

### Общие команды

#### `/start` или `/старт`
Регистрация в системе или приветствие для существующих пользователей.

**Пример:**
```
/start
```

**Ответ:**
```
👋 Привет, Иван!

Вы зарегистрированы как: employee

Используйте /help для списка команд.
```

#### `/help` или `/помощь`
Показывает список доступных команд в зависимости от вашей роли.

**Пример:**
```
/help
```

### Команды для работы с задачами

#### `/my_tasks` или `/мои_задачи`
Показывает список ваших назначенных задач.

**Пример:**
```
/my_tasks
```

**Ответ:**
```
📋 Ваши задачи (2):

📌 ID 1: Подготовить отчет
   Статус: in_progress
   Приоритет: high
   Срок: 2025-12-25

📌 ID 2: Проверить данные
   Статус: pending
   Приоритет: medium
   Срок: 2025-12-20
```

#### `/task_details <ID>` или `/детали_задачи <ID>`
Показывает подробную информацию о задаче.

**Пример:**
```
/task_details 1
```

**Ответ:**
```
📋 Детали задачи #1

Название: Подготовить отчет
Описание: Квартальный отчет по продажам
Статус: in_progress
Приоритет: high
Срок: 2025-12-25
Назначена: User #5
Создана: 2025-11-15 10:30
```

#### `/update_status <ID> <статус>` или `/обновить_статус <ID> <статус>`
Обновляет статус задачи.

**Доступные статусы:**
- `pending` - ожидает
- `in_progress` - в работе
- `completed` - завершена
- `rejected` - отклонена

**Пример:**
```
/update_status 1 in_progress
```

**Ответ:**
```
✅ Статус задачи #1 обновлён на: in_progress
```

### Команды администратора

#### `/create_task` или `/создать_задачу`
Создает новую задачу. Доступно только администраторам.

**Формат:**
```
/create_task title:"название" description:"описание" priority:приоритет due_date:YYYY-MM-DD assigned_to:telegram_id
```

**Параметры:**
- `title:"..."` - название задачи (обязательно)
- `description:"..."` - описание (опционально)
- `priority:` - приоритет: low/medium/high/urgent (по умолчанию: medium)
- `due_date:` - срок в формате YYYY-MM-DD (опционально)
- `assigned_to:` - telegram_id сотрудника (опционально, по умолчанию: вам)

**Примеры:**

Простая задача:
```
/create_task title:"Подготовить отчет"
```

Полная задача:
```
/create_task title:"Подготовить отчет" description:"Квартальный отчет" priority:high due_date:2025-12-25
```

Задача с назначением другому пользователю:
```
/create_task title:"Проверить данные" assigned_to:123456789 priority:urgent
```

**Ответ:**
```
✅ Задача создана успешно!

ID: 3
Название: Подготовить отчет
Приоритет: high
Статус: pending
Назначена: User #5 (Telegram ID: 123456789)
```

#### `/all_tasks` или `/все_задачи`
Показывает все задачи в системе. Доступно только администраторам.

**Пример:**
```
/all_tasks
```

**Ответ:**
```
📋 Все задачи (5):

📌 ID 1: Подготовить отчет
   Статус: completed
   Приоритет: high
   Назначена: User #3

📌 ID 2: Проверить данные
   Статус: in_progress
   Приоритет: medium
   Назначена: User #5
```

## Получение роли администратора

По умолчанию все новые пользователи регистрируются как **employee** (сотрудник).

### Способ 1: SQL запрос

Выполните в Database pane:

```sql
UPDATE users SET role = 'admin' WHERE telegram_id = 'ваш_telegram_id';
```

### Способ 2: Узнать свой Telegram ID

1. Отправьте `/start` боту
2. Откройте Database pane в Replit
3. Выполните запрос:
```sql
SELECT * FROM users ORDER BY created_at DESC LIMIT 5;
```
4. Найдите свою запись по username
5. Обновите роль:
```sql
UPDATE users SET role = 'admin' WHERE id = YOUR_ID;
```

## Архитектура

### Технологии

- **Python 3.11** - язык программирования
- **aiogram 3.22** - асинхронная библиотека для Telegram Bot API
- **PostgreSQL** - база данных
- **psycopg2** - синхронный драйвер PostgreSQL
- **asyncio** - асинхронное программирование

### Структура

```
bot.py           # Основной файл бота
├── Handlers     # Обработчики команд
│   ├── /start
│   ├── /help
│   ├── /my_tasks
│   ├── /all_tasks
│   ├── /task_details
│   ├── /update_status
│   └── /create_task
└── Database     # Работа с PostgreSQL
    ├── get_or_create_user()
    └── get_db_connection()
```

### Схема базы данных

**Таблица users:**
- `id` - уникальный идентификатор
- `telegram_id` - ID пользователя в Telegram (уникальный)
- `username` - имя пользователя
- `role` - роль (admin | employee)
- `created_at` - дата регистрации
- `updated_at` - дата последнего обновления

**Таблица tasks:**
- `id` - уникальный идентификатор
- `title` - название задачи
- `description` - описание
- `priority` - приоритет (low | medium | high | urgent)
- `status` - статус (pending | in_progress | completed | rejected)
- `due_date` - срок выполнения
- `assigned_to_id` - назначена сотруднику (FK → users.id)
- `created_by_id` - создана администратором (FK → users.id)
- `created_at` - дата создания
- `updated_at` - дата последнего обновления

## Преимущества

✅ **Polling вместо webhook** - работает сразу, не нужна публикация  
✅ **Асинхронность** - aiogram использует asyncio для высокой производительности  
✅ **Полностью бесплатно** - не требует оплаты AI API  
✅ **Простота** - понятный код в одном файле  
✅ **Быстрая работа** - мгновенная обработка команд  
✅ **Надёжность** - aiogram - проверенная библиотека с большим сообществом  

## Отличия от webhook-версии

### Polling (текущая версия)
- ✅ Работает сразу после запуска
- ✅ Не требует публикации
- ✅ Не требует HTTPS
- ✅ Проще настроить
- ❌ Постоянно опрашивает API
- ❌ Нужен постоянно запущенный процесс

### Webhook (альтернатива)
- ✅ Telegram сам отправляет обновления
- ✅ Меньше нагрузки на API
- ❌ Требует публикации
- ❌ Требует HTTPS
- ❌ Сложнее настроить

## Тестирование

### Локальное тестирование

Просто запустите бота и откройте его в Telegram:

```bash
python bot.py
```

Затем в Telegram:
1. Найдите вашего бота по username
2. Отправьте `/start`
3. Попробуйте другие команды

### Проверка логов

Логи сохраняются в `/tmp/bot_aiogram.log`:

```bash
tail -f /tmp/bot_aiogram.log
```

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что бот запущен:
```bash
ps aux | grep "python bot.py"
```

2. Проверьте логи:
```bash
tail -50 /tmp/bot_aiogram.log
```

3. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный:
```bash
echo $TELEGRAM_BOT_TOKEN
```

4. Перезапустите бота:
```bash
pkill -f "python bot.py"
python bot.py > /tmp/bot_aiogram.log 2>&1 &
```

### База данных не работает

Проверьте подключение:
```bash
psql $DATABASE_URL -c "SELECT COUNT(*) FROM users;"
```

Если таблицы не созданы:
```bash
npm run db:push
```

### Ошибка "Unauthorized"

Проверьте, что токен бота правильный:
1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите "API Token"
5. Скопируйте токен и обновите в Secrets

## Развертывание

### Для постоянной работы

Бот работает в режиме polling, поэтому процесс должен быть всегда запущен.

**Опция 1: Использовать Replit (рекомендуется)**
- Replit автоматически перезапускает процессы
- Бот будет работать постоянно

**Опция 2: Использовать systemd (Linux сервер)**
Создайте файл `/etc/systemd/system/telegram-bot.service`:
```ini
[Unit]
Description=Telegram Task Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## Дополнительные функции (TODO)

- [ ] Добавить inline-кнопки для быстрых действий
- [ ] Напоминания о дедлайнах
- [ ] Статистика по задачам
- [ ] Экспорт отчётов
- [ ] Фильтрация задач по статусу/приоритету
- [ ] Поиск задач по ключевым словам

## Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f /tmp/bot_aiogram.log`
2. Убедитесь, что environment variables установлены
3. Проверьте подключение к базе данных
4. Перезапустите бота

## Лицензия

MIT

---

**Разработано с ❤️ на Replit**

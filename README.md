# Telegram Бот для Управления Задачами (Python + Aiogram + Inline-кнопки)

## Описание

Полноценный асинхронный Telegram бот на Python с **inline-кнопками** для удобного управления задачами. Бот работает по командам и использует **polling** (без webhook) - **полностью бесплатно**!

## Особенности

✨ **Inline-кнопки** - все команды доступны через удобные кнопки  
⚡ **Асинхронность** - быстрая обработка через aiogram  
🔄 **Polling** - работает сразу, без публикации  
💰 **Бесплатно** - без затрат на AI API  
🎨 **Эмодзи** - визуальные индикаторы статусов и приоритетов  

## Возможности

### Для Администраторов
- ➕ Создание задач через интерактивный диалог
- 📊 Просмотр всех задач в системе
- 🔄 Изменение статусов любых задач
- 👥 Назначение задач себе (расширение на других пользователей - в планах)

### Для Сотрудников
- 📋 Просмотр своих задач
- 🔄 Изменение статусов своих задач
- 👀 Детальная информация по каждой задаче

## Визуальные индикаторы

### Статусы задач
- ⏳ Ожидает (pending)
- 🔄 В работе (in_progress)
- ✅ Завершена (completed)
- ❌ Отклонена (rejected)

### Приоритеты
- 🔴 Срочно (urgent)
- 🟠 Высокий (high)
- 🟡 Средний (medium)
- 🟢 Низкий (low)

## Установка и Запуск

### 1. Зависимости

Установлены через `uv`:
- **aiogram** 3.22.0 - асинхронная библиотека для Telegram Bot API
- **psycopg2-binary** - PostgreSQL драйвер
- **asyncpg** - асинхронный PostgreSQL драйвер
- **python-dotenv** - загрузка environment variables

### 2. Environment Variables

```bash
DATABASE_URL=<PostgreSQL connection string>
TELEGRAM_BOT_TOKEN=<ваш Telegram bot token>
```

### 3. Запуск бота

```bash
python bot.py
```

Или используйте скрипт:
```bash
./START_BOT.sh
```

Бот будет работать в режиме **polling** (постоянно опрашивает Telegram API).

## Использование

### Главное меню

После отправки `/start` вы увидите главное меню с кнопками:

**Для всех:**
- 📋 **Мои задачи** - список ваших задач
- ❓ **Помощь** - справка

**Для администраторов:**
- 📊 **Все задачи** - все задачи в системе
- ➕ **Создать задачу** - добавить новую задачу

### Работа с задачами

1. **Просмотр задач**
   - Нажмите "📋 Мои задачи"
   - Выберите задачу из списка
   - Увидите полную информацию

2. **Изменение статуса**
   - Откройте задачу
   - Нажмите на кнопку нужного статуса
   - Статус обновится мгновенно

3. **Создание задачи** (только admin)
   - Нажмите "➕ Создать задачу"
   - Введите название
   - Введите описание (или пропустите)
   - Выберите приоритет из кнопок
   - Задача создана!

## Получение роли администратора

По умолчанию все новые пользователи - **сотрудники**.

Для получения роли администратора:

```sql
UPDATE users SET role = 'admin' WHERE telegram_id = 'ваш_telegram_id';
```

Узнать свой Telegram ID:
1. Отправьте `/start` боту
2. Откройте Database pane в Replit
3. Выполните: `SELECT * FROM users ORDER BY created_at DESC LIMIT 5;`
4. Найдите свою запись

## Архитектура

### Технологии

- **Python 3.11** - язык программирования
- **aiogram 3.22** - асинхронная библиотека для Telegram Bot API
- **PostgreSQL** - база данных
- **psycopg2** - синхронный драйвер PostgreSQL
- **FSM (Finite State Machine)** - для создания задач через диалог

### Inline-кнопки

Бот использует `InlineKeyboardMarkup` и `InlineKeyboardButton` для создания интерактивного интерфейса:

- **Главное меню** - основные действия
- **Список задач** - каждая задача - кнопка
- **Детали задачи** - кнопки для смены статуса
- **Создание задачи** - пошаговый диалог с кнопками

### Callback Handlers

Обработчики callback запросов:
- `callback_help` - справка
- `callback_my_tasks` - список задач
- `callback_all_tasks` - все задачи (admin)
- `callback_task_details` - детали задачи
- `callback_update_status` - обновление статуса
- `callback_create_task` - создание задачи
- `callback_cancel` - отмена операции
- `callback_back_to_main` - возврат в меню

### База данных

**Таблица users:**
- `id` - уникальный идентификатор
- `telegram_id` - ID пользователя в Telegram (уникальный)
- `username` - имя пользователя
- `role` - роль (admin | employee)
- `created_at`, `updated_at` - временные метки

**Таблица tasks:**
- `id` - уникальный идентификатор
- `title` - название задачи
- `description` - описание
- `priority` - приоритет (low | medium | high | urgent)
- `status` - статус (pending | in_progress | completed | rejected)
- `due_date` - срок выполнения
- `assigned_to_id` - назначена сотруднику (FK → users.id)
- `created_by_id` - создана администратором (FK → users.id)
- `created_at`, `updated_at` - временные метки

## Преимущества

✅ **Inline-кнопки** - удобный интерфейс, не нужно вводить команды  
✅ **Визуальные индикаторы** - эмодзи для быстрого понимания статуса  
✅ **Polling** - работает сразу после запуска  
✅ **Асинхронность** - высокая производительность  
✅ **Бесплатно** - без затрат на AI API  
✅ **FSM** - пошаговое создание задач  
✅ **Простота** - весь код в одном файле  

## Управление ботом

### Запуск
```bash
python bot.py
# или
./START_BOT.sh
```

### Проверка статуса
```bash
ps aux | grep "python bot.py"
```

### Просмотр логов
```bash
tail -f /tmp/telegram_bot.log
```

### Остановка
```bash
pkill -f "python bot.py"
```

## Тестирование

Просто откройте Telegram и:

1. Найдите вашего бота по username
2. Отправьте `/start`
3. Нажимайте на кнопки!

## Устранение неполадок

### Бот не отвечает

1. Проверьте процесс:
```bash
ps aux | grep "python bot.py"
```

2. Перезапустите:
```bash
pkill -f "python bot.py"
python bot.py > /tmp/telegram_bot.log 2>&1 &
```

3. Проверьте логи:
```bash
tail -50 /tmp/telegram_bot.log
```

### Кнопки не работают

Убедитесь, что:
- Бот запущен
- У вас последняя версия Telegram
- Вы отправили `/start` после обновления бота

## Будущие улучшения

- [ ] Назначение задач другим пользователям через inline-кнопки
- [ ] Календарь для выбора срока выполнения
- [ ] Фильтры задач (по статусу, приоритету)
- [ ] Поиск задач
- [ ] Напоминания о дедлайнах
- [ ] Статистика с графиками
- [ ] Экспорт отчётов

## Файловая структура

```
.
├── bot.py              # Основной файл бота с inline-кнопками
├── START_BOT.sh        # Скрипт запуска
├── README.md           # Эта документация
├── replit.md           # Архитектура проекта
└── shared/
    ├── schema.ts       # Схема БД (Drizzle)
    └── db.ts           # Подключение к БД
```

## Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f /tmp/telegram_bot.log`
2. Убедитесь, что environment variables установлены
3. Перезапустите бота: `./START_BOT.sh`

---

**Разработано с ❤️ на Replit с использованием aiogram**

# TaskMasterBot - Telegram Бот для Управления Задачами

Полнофункциональный асинхронный Telegram бот на Python для управления задачами в команде. Бот использует aiogram 3.x и SQLite для хранения данных.

## 🚀 Особенности

- ✨ **Inline-кнопки** - удобный интерфейс управления через кнопки
- ⚡ **Асинхронность** - быстрая обработка запросов через aiogram
- 🔄 **Polling** - работает без необходимости публикации webhook
- 📊 **Статистика и отчёты** - дашборд с аналитикой для администраторов
- 🔔 **Уведомления** - автоматические напоминания о дедлайнах
- 📸 **Прикрепление фото** - возможность добавлять изображения к задачам
- 💬 **Комментарии** - обсуждение задач с комментариями
- 📝 **История изменений** - полный лог всех действий с задачами
- 🔍 **Поиск задач** - быстрый поиск по названию и описанию
- 👥 **Управление пользователями** - система ролей (администратор/сотрудник)
- 🎨 **Визуальные индикаторы** - эмодзи для статусов и приоритетов

## 📋 Возможности

### Для Администраторов

- ➕ Создание задач с указанием приоритета, дедлайна и исполнителя
- 📊 Просмотр всех задач в системе
- 🔄 Изменение статусов любых задач
- 👥 Управление пользователями (добавление/удаление администраторов и сотрудников)
- 📈 Статистика и отчёты (дашборд с аналитикой)
- 🗑️ Удаление задач
- 📸 Прикрепление фото к задачам
- 💬 Комментирование задач
- 🔍 Поиск задач

### Для Сотрудников

- 📋 Просмотр своих задач
- 🔄 Изменение статусов своих задач
- 👀 Детальная информация по каждой задаче
- 📸 Просмотр прикреплённых фото
- 💬 Добавление комментариев
- 🔍 Поиск своих задач

## 🎯 Статусы задач

- ⏳ **Ожидает** (pending) - задача создана, но ещё не начата
- 🔄 **В работе** (in_progress) - задача выполняется
- 🔶 **Частично завершена** (partially_completed) - задача выполнена частично
- ✅ **Завершена** (completed) - задача полностью выполнена
- ❌ **Отклонена** (rejected) - задача отклонена

## 🔴 Приоритеты задач

- 🔴 **Срочно** (urgent) - высший приоритет
- 🟠 **Высокий** (high) - высокий приоритет
- 🟡 **Средний** (medium) - средний приоритет (по умолчанию)
- 🟢 **Низкий** (low) - низкий приоритет

## 🛠️ Технологии

- **Python 3.11+** - язык программирования
- **aiogram 3.22+** - асинхронная библиотека для Telegram Bot API
- **SQLite** - база данных
- **python-dotenv** - управление переменными окружения
- **pytz** - работа с часовыми поясами
- **matplotlib** - генерация графиков для статистики
- **openpyxl** - экспорт отчётов в Excel
- **Pillow** - обработка изображений

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/TaskMasterBot.git
cd TaskMasterBot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или используйте `uv`:

```bash
uv sync
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
DATABASE_PATH=data/task_bot.db
TIMEZONE=Europe/Kaliningrad
TIMEZONE_ABBR=КЛД
LOG_LEVEL=INFO
```

**Как получить токен бота:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### 4. Инициализация базы данных

База данных создастся автоматически при первом запуске бота. Но если нужно добавить первого администратора вручную:

```bash
python init_admin.py
```

### 5. Запуск бота

**Простой запуск:**
```bash
python bot.py
```

**Или используйте скрипт управления:**
```bash
chmod +x START_BOT.sh
./START_BOT.sh
```

Скрипт предоставляет интерактивное меню для управления ботом:
- Запуск/остановка/перезапуск бота
- Просмотр статуса и логов
- Установка systemd службы (для Linux)

## 🏗️ Структура проекта

```
TaskMasterBot/
├── app/
│   ├── handlers/          # Обработчики команд и callback'ов
│   │   ├── core.py        # Основные команды и меню
│   │   ├── statuses.py    # Управление статусами
│   │   ├── photos.py      # Работа с фотографиями
│   │   ├── comments.py    # Комментарии к задачам
│   │   ├── history.py     # История изменений
│   │   └── notification_settings.py  # Настройки уведомлений
│   ├── keyboards/         # Клавиатуры для бота
│   │   ├── main_menu.py   # Главное меню
│   │   ├── task_keyboards.py  # Клавиатуры для задач
│   │   └── user_keyboards.py  # Клавиатуры для пользователей
│   ├── services/          # Бизнес-логика
│   │   ├── tasks.py       # Сервис задач
│   │   ├── users.py       # Сервис пользователей
│   │   ├── notifications.py  # Уведомления
│   │   ├── statistics.py  # Статистика
│   │   ├── task_history.py  # История задач
│   │   └── comments.py    # Сервис комментариев
│   ├── config.py          # Конфигурация
│   ├── database.py        # Работа с БД
│   ├── logging_config.py # Настройка логирования
│   ├── main.py            # Инициализация бота
│   └── states.py          # FSM состояния
├── data/                  # Папка для базы данных
├── logs/                  # Папка для логов
├── bot.py                 # Точка входа
├── init_admin.py          # Скрипт инициализации администратора
├── migrate_to_timestamp.py  # Скрипт миграции БД
├── START_BOT.sh           # Скрипт управления ботом
├── requirements.txt       # Зависимости Python
└── README.md              # Документация
```

## 🔐 Система авторизации

Бот использует whitelist-систему авторизации. Только пользователи, добавленные в базу данных, могут использовать бота.

**Добавление первого администратора:**

1. Запустите скрипт:
   ```bash
   python init_admin.py
   ```

2. Введите Telegram username администратора (без @)

3. После этого администратор может:
   - Запустить бота командой `/start`
   - Добавлять других пользователей через интерфейс бота

**Добавление пользователей через бота:**

Администраторы могут добавлять новых пользователей через меню:
- "➕👨‍💼 Добавить админа" - добавить администратора
- "➕👤 Добавить сотрудника" - добавить сотрудника

## 📱 Использование

### Запуск бота

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Используйте кнопки для навигации

### Создание задачи (только для администраторов)

1. Нажмите "➕ Создать задачу"
2. Введите название задачи
3. Введите описание (или пропустите)
4. Выберите приоритет из предложенных кнопок
5. Выберите исполнителя из списка
6. Установите дедлайн (дату и время)
7. При необходимости прикрепите фото
8. Задача создана!

### Работа с задачами

- **Просмотр задач**: Нажмите "📋 Мои задачи" или "📊 Все задачи"
- **Детали задачи**: Нажмите на задачу в списке
- **Изменение статуса**: Используйте кнопки статусов в деталях задачи
- **Добавление комментария**: Нажмите "💬 Комментарии" в деталях задачи
- **Просмотр истории**: Нажмите "📜 История" в деталях задачи

### Поиск задач

1. Нажмите "🔍 Поиск"
2. Введите ключевое слово
3. Просмотрите результаты поиска

## 🔔 Настройки уведомлений

Пользователи могут настроить уведомления о:
- Новых задачах
- Изменениях статусов
- Приближающихся дедлайнах
- Просроченных задачах

Доступ через меню: "🔔 Настройки"

## 📊 Статистика и отчёты

Администраторы могут просматривать:
- Общее количество задач
- Задачи по статусам
- Просроченные задачи
- Статистику за период
- Графики и диаграммы
- Экспорт в Excel

Доступ через меню: "📈 Статистика и отчёты"

## 🗄️ База данных

Бот использует SQLite для хранения данных. База данных создаётся автоматически при первом запуске.

**Основные таблицы:**
- `users` - пользователи системы
- `allowed_users` - whitelist авторизованных пользователей
- `tasks` - задачи
- `task_comments` - комментарии к задачам
- `task_history` - история изменений задач
- `task_notifications` - уведомления
- `notification_settings` - настройки уведомлений пользователей

## 🚀 Развёртывание на сервере

### Использование systemd (Linux)

1. Запустите скрипт управления:
   ```bash
   sudo ./START_BOT.sh
   ```

2. Выберите опцию "6) Установить systemd службу"

3. Управление службой:
   ```bash
   sudo systemctl start telegram-task-bot
   sudo systemctl stop telegram-task-bot
   sudo systemctl restart telegram-task-bot
   sudo systemctl status telegram-task-bot
   ```

4. Просмотр логов:
   ```bash
   journalctl -u telegram-task-bot -f
   ```

### Ручной запуск в фоне

```bash
nohup python3 bot.py > logs/bot.log 2>&1 &
```

## 📝 Логирование

Логи сохраняются в папке `logs/`:
- `bot.log` - основные логи бота
- `bot_run.log` - логи запуска
- `bot_restart.log` - логи перезапуска
- `startup.log` - логи инициализации

Настройка уровня логирования через переменную `LOG_LEVEL` в `.env`:
- `DEBUG` - подробные логи
- `INFO` - информационные сообщения (по умолчанию)
- `WARNING` - предупреждения
- `ERROR` - только ошибки

## 🔧 Миграция базы данных

Если вы обновляете бота со старой версии, может потребоваться миграция:

```bash
python migrate_to_timestamp.py
```

**Внимание:** Скрипт создаст резервную копию базы данных перед миграцией.

## 🐛 Устранение неполадок

### Бот не отвечает

1. Проверьте, запущен ли процесс:
   ```bash
   ps aux | grep "python.*bot.py"
   ```

2. Проверьте логи:
   ```bash
   tail -50 logs/bot.log
   ```

3. Проверьте токен в `.env` файле

4. Перезапустите бота:
   ```bash
   ./START_BOT.sh
   ```

### Ошибка подключения к базе данных

1. Убедитесь, что папка `data/` существует:
   ```bash
   mkdir -p data
   ```

2. Проверьте права доступа к папке

### Пользователь не может использовать бота

1. Убедитесь, что пользователь добавлен в whitelist
2. Проверьте, что у пользователя есть username в Telegram
3. Используйте `init_admin.py` для добавления первого администратора

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `tail -f logs/bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте переменные окружения в `.env`
4. Создайте Issue в репозитории GitHub


**Разработано с ❤️ используя Python и aiogram**

